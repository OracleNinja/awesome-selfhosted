# Working artefacts. Manifests, gate reports and review packages are the
# auditable record and belong in version control; generated artwork, built
# PDFs and publication packages do not — they are reproducible from the
# manifest, and the build is deterministic.
books/*/assets/
books/*/build/
__pycache__/
.pytest_cache/
