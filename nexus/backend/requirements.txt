# NEXUS backend runtime dependencies.
#
# Pinned to compatible ranges rather than exact versions so security patches
# land without a code change; the lock for reproducible deploys is the
# container image digest plus `pip freeze` output captured at build time
# (see DEPLOYMENT.md).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<0.31
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
argon2-cffi>=23.1,<26.0
httpx>=0.27,<0.29
