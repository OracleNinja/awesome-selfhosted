/**
 * The default tool timeout lives in `@jarvis/shared` — see the rationale there.
 * Re-exported here so tool authors find it next to the registry.
 */
export { DEFAULT_TOOL_TIMEOUT_MS } from '@jarvis/shared';
