#!/usr/bin/env bash
# Run the Dealer Arbitrage CLI without installing anything.
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHONPATH="$here/src${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m dealer_arbitrage "$@"
