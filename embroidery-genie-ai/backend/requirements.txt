# Embroidery Genie AI — backend dependencies
# Versions below are the ones the test suite is verified against.

# --- Web framework -----------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
pydantic[email]==2.13.4
pydantic-settings==2.14.2

# --- Database ----------------------------------------------------------------
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
greenlet==3.5.4

# --- Auth (Supabase JWT verification) ---------------------------------------
pyjwt[crypto]==2.13.0
httpx==0.28.1

# --- Imaging / geometry ------------------------------------------------------
numpy==2.4.6
opencv-python-headless==5.0.0.93
pillow==12.3.0
pillow-heif==1.5.0
shapely==2.1.2

# --- Embroidery formats ------------------------------------------------------
# Native DST/EXP writers ship in app/embroidery/writers; pyembroidery covers
# PES, JEF, VP3, XXX, U01 and PEC. See writers/base.py for the rationale.
pyembroidery==1.5.1

# --- Documents (thread chart, production notes) ------------------------------
reportlab==5.0.0

# --- AI vision (optional; the analyzer degrades to pure CV without keys) ------
openai==1.59.6
anthropic==0.42.0

# --- Testing -----------------------------------------------------------------
pytest==9.0.2
